/// Responsive column configuration for form layouts
class ResponsiveColumns {
  final int mobile;
  final int tablet;
  final int desktop;

  const ResponsiveColumns({this.mobile = 1, this.tablet = 2, this.desktop = 3});
}
