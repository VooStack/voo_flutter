import 'package:flutter/material.dart';

class VooGroupedList extends StatelessWidget {
  const VooGroupedList({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}