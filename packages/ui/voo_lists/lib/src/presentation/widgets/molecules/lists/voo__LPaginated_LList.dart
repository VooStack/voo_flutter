import 'package:flutter/material.dart';

class VooPaginatedList extends StatelessWidget {
  const VooPaginatedList({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}
