import 'package:flutter/material.dart';

class VooResponsiveGap extends StatelessWidget {
  const VooResponsiveGap({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}
