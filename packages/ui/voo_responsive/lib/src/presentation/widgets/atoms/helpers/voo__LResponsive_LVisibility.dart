import 'package:flutter/material.dart';

class VooResponsiveVisibility extends StatelessWidget {
  const VooResponsiveVisibility({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}
